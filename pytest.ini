[pytest]
markers =
    integration: Integration tests that test complete workflows
    unit: Unit tests for individual functions
    validation: Tests for data validation rules

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = 
    -v
    --strict-markers
    --tb=short
